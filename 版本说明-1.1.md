# 1.1 改动说明

相对 1.0，这个版本主要修了手机、iPad 和衍生详情页的几处体验问题。GitHub 的 `1.1` 只做备份；线上仍部署到 Cloudflare 的 `main` 地址。

## 改了什么

- **手机端导出按钮同一行**  
  广场里的 6 个导出相关按钮（导出全部、导出所选、导出作者、导出分类、导出收藏、屏蔽不喜欢）尽量排在同一行。宽度不够时，这一行自己左右滑，不会把整页撑出横向滚动。

- **iPad「一行几个」可以选到 3**  
  iPad 不再被误判成手机。平板上「一行几个」是 1 / 2 / 3，不再被锁成最多 2 列。

- **衍生详情的「上一条 / 下一条」按不同小剧场切换**  
  从衍生列表点进详情后，上一条 / 下一条是「小剧场1 → 小剧场2」这种组与组之间的切换。  
  同一篇的原文 / 版本1 / 版本2，仍用详情页里的版本按钮切换。  
  从广场卡片点进去，上下条还是广场列表顺序。

- **部署固定走 Cloudflare `main`**  
  线上地址继续用：https://main.mini-theater-review-loop.pages.dev  
  GitHub 的 `1.0`、`1.1` 只做代码备份，不再当成 Cloudflare 的分支名。  
  以后可用 `npm run deploy` 发布到 `main`。

## 没改什么

- 数据库数据和已有小剧场内容
- 广场、上传、审核后台的主要功能
- GitHub 备份仍然不包含 `node_modules`、`dist`、`.wrangler`