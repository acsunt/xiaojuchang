# 1.2 改动说明

相对 1.1，这个版本主要改了审核后台：搜索更好用，并去掉了「背景」栏目。GitHub 的 `1.2` 只做备份。

## 改了什么

- **审核后台默认从全部内容里搜**  
  输入关键词后，不再只搜当前这个状态页，而是从全部小剧场里搜。  
  没输入关键词时，列表还是按「全部 / 待审核 / 已通过 / 已拒绝 / 已下线」筛选。

- **搜索框下面可以点选字段**  
  小剧场可点：标题、作者、分类、正文。  
  没点选时，这几个字段都会搜。点选后，只搜选中的字段。

- **repo 审核也支持搜索**  
  repo 页同样默认从全部 repo 里搜。  
  可点选：昵称、标题、作者、正文。  
  「通过当前待审核」会跟着当前搜索结果走，搜不到待审核项时按钮会禁用。

- **删掉审核后台的「背景」栏目**  
  顶部不再有「背景」标签，也不能再在后台改日间 / 夜间、电脑端 / 手机端背景图。  
  游客侧如果以前配过背景，还会继续显示，只是后台现在改不了。

- **本机部署命令不再用自定义配置路径**  
  Cloudflare Pages 不接受 `--config`。  
  现在用 `npm run deploy` 发布到 Pages 的 main 别名。  
  GitHub 上的 `wrangler.toml` 仍是占位内容；真实项目名和数据库 ID 只留在本机，不上传。

## 没改什么

- 数据库数据和已有小剧场、repo 内容
- 广场、上传、衍生详情等游客侧功能
- GitHub 备份仍然不包含 `node_modules`、`dist`、`.wrangler` 和本机真实配置